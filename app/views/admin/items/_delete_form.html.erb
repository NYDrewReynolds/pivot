<%= form_for(@item, html: {multipart: true, method: 'delete'}, :url => restaurant_admin_item_path(current_restaurant, @item) ) do |f| %>
 <%= f.submit "Delete This Item", class: 'btn btn-danger btn-lg' %>
 <% end %>
