<h2>Edit Categories</h2>
<table>
		<% @categories.each do |category| %>
		<tr>
			<td><%= link_to category.title, edit_restaurant_admin_category_path(owned_restaurant, category) %></td>
		</tr>
		<% end %>
</table>
