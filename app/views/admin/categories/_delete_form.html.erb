<%= form_for(@category, html: {multipart: true, method: 'delete'}, :url => restaurant_admin_category_path(current_restaurant, @category) ) do |f| %>
 <%= f.submit "Delete This Item", class: 'btn btn-danger btn-lg' %>
<% end %>
